[user]
    name = Gildas Quéméner
    email = gildas.quemener@gmail.com
    signinkey = B9976FC98B7ABF43
[alias]
    co = checkout
    lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --
    dw = diff --word-diff
    st = status --branch -uall
    standup = log --since yesterday --author gildas.quemener@gmail.com --pretty=oneline
[color]
    ui = auto
[color "status"]
    added = green
    changed = yellow
    untracked = red
[core]
    excludesfile = ~/.gitignore_global
    editor = vim
    whitespace = trailing-space,space-before-tab
    attributesfile = ~/.gitattributes
    autocrlf = input
[web]
    browser = firefox
[push]
    default = current
[mergetool]
    keepBackup = false
    keepTemporaries = false
    prompt = false
[log]
    abbrevCommit = true
[diff]
    mnemonicPrefix = true
[diff "exif"]
	textconv = exiftool
[commit]
[pull]
	rebase = false
